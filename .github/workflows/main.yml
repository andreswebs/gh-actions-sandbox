---
name: Test

on:

  push:
    branches:
      - main

  pull_request:
    branches:
      - "*"

jobs:

  example:

    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:

      - name: Inspect
        env:
          EVENT_STATUS: ${{ github.event_name != 'pull_request' }}
        run: |
          echo "event-status: ${EVENT_STATUS}"
